<h1>Recetame App</h1>
<%= link_to t('button.recipe.create'), new_recipe_path %></a>
<h2>Aquí están las últimas 10 recetas</h2>

<ul id="recipes" style= 'list-style: none'>
  <% @recipes.each do |recipe| %>

    <li class="home_recipe"><%= recipe.title %></li>

  <% end %>
</ul>

<h2>Y aquí los últimos 5 "libros"</h2>

<ul id="books" style= 'list-style: none'>
  <% @books.each do |book| %>

   <li class="home_book"><%= book.title %></li>
   <% end %>
</ul>

<%= link_to "Cookpad: para inspirarnos un poquillo", 'https://cookpad.com/us?via=jp' %>
<p>Find me in app/views/static_pages/home.html.erb</p>
